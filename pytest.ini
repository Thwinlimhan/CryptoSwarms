[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests that require live services (redis, timescaledb)
    slow: marks unusually slow tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
